# SPM Simulator

**热轧平整机组排程沙盘模拟系统**

一个基于 Tauri + React + Rust 的桌面应用程序，用于热轧平整机组的生产排程模拟与优化。

[![Tests](https://img.shields.io/badge/tests-368%20passed-success)](https://github.com)
[![Coverage](https://img.shields.io/badge/coverage-85.53%25-brightgreen)](https://github.com)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.9-blue)](https://www.typescriptlang.org/)
[![Rust](https://img.shields.io/badge/Rust-1.77%2B-orange)](https://www.rust-lang.org/)
[![License](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)

---

## 📋 目录

- [功能特性](#-功能特性)
- [技术栈](#-技术栈)
- [快速开始](#-快速开始)
- [开发指南](#-开发指南)
- [测试指南](#-测试指南)
- [项目结构](#-项目结构)
- [构建部署](#-构建部署)
- [常见问题](#-常见问题)

---

## ✨ 功能特性

### 核心功能

- **智能排程**: 基于多维度优先级和约束条件的自动排程算法
- **风险分析**: 实时评估排程方案的风险指标（宽跳、换辊、适温等）
- **方案对比**: 支持多方案并行对比，可视化差异分析
- **历史追溯**: 完整的版本历史记录和回滚功能
- **数据管理**: 材料导入、字段映射、数据备份恢复

### 特色功能

- **适温管理**: 智能计算材料适温状态，优化生产效率
- **拖拽调序**: 支持列表和甘特图双模式拖拽调整
- **实时验证**: 排程过程中实时验证硬约束和软约束
- **灵活配置**: 可自定义优先级权重、约束条件、评估指标

---

## 🛠 技术栈

### 前端

- **框架**: React 19.2.0
- **语言**: TypeScript 5.9.3
- **构建工具**: Vite 7.3.1
- **UI 组件**: Ant Design 6.3.0
- **状态管理**: Zustand 5.0.11
- **路由**: React Router 7.13.0
- **图表**: ECharts 6.0.0
- **测试**: Vitest 4.0.18 + Playwright 1.58.2

### 后端

- **框架**: Tauri 2.10.0
- **语言**: Rust 1.77.2+
- **数据库**: SQLite
- **ORM**: SeaORM 1.1
- **异步运行时**: Tokio 1.x

---

## 🚀 快速开始

### 环境要求

- **Node.js**: 18.x 或更高版本
- **Rust**: 1.77.2 或更高版本
- **操作系统**: Windows 10+, macOS 10.15+, Linux

### 安装步骤

1. **克隆仓库**

```bash
git clone <repository-url>
cd spm-simulator
```

2. **安装前端依赖**

```bash
npm install
```

3. **安装 Rust 工具链** (如果尚未安装)

```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

4. **启动开发服务器**

```bash
npm run tauri dev
```

应用程序将在开发模式下启动，支持热重载。

---

## 💻 开发指南

### 开发命令

```bash
# 启动开发服务器
npm run dev

# 启动 Tauri 开发模式
npm run tauri dev

# 运行单元测试
npm run test:unit

# 运行测试并生成覆盖率报告
npm run test:unit:coverage

# 运行 E2E 测试
npm run test:e2e

# 运行后端测试
npm run test:backend

# 代码检查
npm run lint

# Rust 代码检查
npm run lint:rust

# 格式化 Rust 代码
npm run fmt:rust

# 完整质量检查
npm run check:quality
```

### 项目规范

#### 代码风格

- **TypeScript**: 遵循 ESLint 配置，使用严格模式
- **Rust**: 遵循 Clippy 规范，使用 rustfmt 格式化
- **提交**: 使用 Husky + lint-staged 进行 pre-commit 检查

#### 分支策略

- `main`: 主分支，保持稳定
- `develop`: 开发分支
- `feature/*`: 功能分支
- `fix/*`: 修复分支

#### 提交规范

```
feat: 新功能
fix: 修复 bug
docs: 文档更新
style: 代码格式调整
refactor: 重构
test: 测试相关
chore: 构建/工具链相关
```

---

## 🧪 测试指南

### 测试覆盖率

当前测试覆盖率：

- **行覆盖率**: 85.53%
- **函数覆盖率**: 76.14%
- **分支覆盖率**: 81.73%
- **语句覆盖率**: 87.9%

### 运行测试

#### 前端单元测试

```bash
# 运行所有单元测试
npm run test:unit

# 监听模式
npm run test:unit:watch

# 生成覆盖率报告
npm run test:unit:coverage
```

覆盖率报告位置: `dist/coverage/index.html`

#### E2E 测试

```bash
# 运行所有 E2E 测试
npm run test:e2e

# 运行烟雾测试
npm run test:e2e:smoke
```

#### 后端测试

```bash
# 运行 Rust 测试
npm run test:backend

# 或直接使用 cargo
cd src-tauri
cargo test
```

### 测试文件位置

- **单元测试**: `src/**/*.test.ts(x)`
- **E2E 测试**: `e2e/**/*.spec.ts`
- **后端测试**: `src-tauri/src/**/*.rs` (包含 `#[cfg(test)]`)

---

## 📁 项目结构

```
spm-simulator/
├── src/                      # 前端源代码
│   ├── pages/               # 页面组件
│   │   ├── Workbench/       # 计划工作台（自动排程/手工调整/甘特图）
│   │   ├── RiskOverview/    # 风险概览（风险评估/适温预测）
│   │   ├── Compare/         # 方案对比（双/三方案对比）
│   │   ├── History/         # 历史追溯（版本管理/回滚）
│   │   ├── DataManage/      # 数据管理（备份/恢复/清理）
│   │   ├── FieldMapping/    # 数据映射（字段映射模板管理）
│   │   ├── Strategy/        # 策略配置（排序规则/约束/适温规则）
│   │   ├── Settings/        # 设置中心（系统/班次/优先级/检修）
│   │   └── Logs/            # 日志管理（操作日志查看/导出）
│   ├── components/          # 通用组件（MaterialTable/DeferredEChart/标签组件等）
│   ├── services/            # API 服务层（封装 Tauri invoke，含请求去重缓存）
│   ├── stores/              # 状态管理（Zustand，6 个 store）
│   ├── hooks/               # 全局自定义 Hooks
│   ├── workers/             # Web Workers（优先级/风险/日志后台计算）
│   ├── utils/               # 工具函数
│   ├── types/               # TypeScript 类型定义（7 个模块）
│   └── constants/           # 常量定义
├── src-tauri/               # Tauri 后端（Rust）
│   ├── src/
│   │   ├── commands/        # Tauri 命令层（API 入口，~96 个命令）
│   │   │   ├── schedule/    # 排程相关命令（方案/操作/风险/对比/历史/撤销/日志）
│   │   │   └── config/      # 配置相关命令（策略/优先级/系统/检修）
│   │   ├── engine/          # 核心算法引擎（排程/优先级/评估/验证/换辊/适温）
│   │   ├── models/          # 数据模型（SeaORM，19 个实体）
│   │   ├── services/        # 业务服务层（导入/导出/备份/性能/错误追踪）
│   │   └── migration/       # 数据库迁移脚本
│   └── Cargo.toml           # Rust 依赖配置
├── e2e/                     # E2E 测试（Playwright，6 个 spec）
├── docs/                    # 项目文档（架构/API/组件/部署等）
├── scripts/                 # 构建和性能测试脚本
├── test-data/               # 测试数据（策略模板示例）
└── package.json             # 前端依赖配置
```

### 核心模块说明

#### 前端架构

- **Services 层**: 封装所有 Tauri API 调用，提供统一接口，含请求去重缓存（`invokeDeduped`）
- **Stores 层**: 使用 Zustand 管理全局状态（6 个 store：排程/物料/配置/性能/错误追踪等）
- **Components 层**: React 组件，分页面组件和通用组件，关键组件均用 `React.memo` 优化
- **Workers 层**: Web Workers 处理重计算任务（优先级命中、风险配置命中、日志过滤）

#### 后端架构

- **Commands**: Tauri 命令层，前端通过 `invoke` 调用，约 96 个命令，分排程/配置 2 大子模块
- **Engine**: 核心算法——8 步排程算法、6 维优先级计算、5 维方案评估、约束验证器
- **Services**: 业务逻辑层，处理数据导入/导出（Excel/CSV）、备份恢复、性能监控
- **Models**: SeaORM 实体（19 个），映射 SQLite 数据库表

---

## 📦 构建部署

### 开发构建

```bash
# 构建前端
npm run build

# 构建 Tauri 应用 (开发模式)
npm run tauri build -- --debug
```

### 生产构建

```bash
# 构建生产版本
npm run tauri build
```

构建产物位置:
- **Windows**: `src-tauri/target/release/spm-simulator.exe`
- **macOS**: `src-tauri/target/release/bundle/macos/`
- **Linux**: `src-tauri/target/release/bundle/appimage/`

### 性能检查

```bash
# 检查构建产物大小
npm run perf:bundle

# 运行性能基准测试
npm run perf:risk-benchmark
```

---

## 🔧 配置说明

### 环境变量

项目使用 Tauri 的配置系统，主要配置文件：

- `src-tauri/tauri.conf.json`: Tauri 应用配置
- `vite.config.ts`: Vite 构建配置
- `vitest.config.ts`: 测试配置

### 数据库

应用使用 SQLite 数据库，数据文件位置：

- **开发环境**: `src-tauri/dev.db`
- **生产环境**: 用户数据目录 (由 Tauri 管理)

### 日志

日志文件位置：

- **开发环境**: 控制台输出
- **生产环境**: 用户日志目录 (由 Tauri 管理)

---

## 📖 使用指南

### 基本工作流程

1. **导入材料数据**
   - 进入"数据管理"页面
   - 点击"导入材料"，选择 Excel/CSV 文件
   - 配置字段映射（如需要）
   - 刷新适温状态

2. **创建排程方案**
   - 进入"排程"页面
   - 点击"新建方案"
   - 选择策略模板
   - 点击"自动排程"

3. **调整和优化**
   - 在列表或甘特图中拖拽调整顺序
   - 查看风险分析结果
   - 使用方案对比功能评估不同方案

4. **导出和备份**
   - 导出排程方案
   - 导出历史报告
   - 备份数据库

### 页面联动

系统支持页面间的参数传递：

- **风险页**: `/risk?planId=123`
- **历史页**: `/history?planId=123`
- **对比页**: `/compare?planA=123&planB=456&planC=789`

---

## ❓ 常见问题

### 开发环境问题

**Q: 启动时报错 "Rust toolchain not found"**

A: 请确保已安装 Rust 工具链：
```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

**Q: npm install 失败**

A: 尝试清除缓存后重新安装：
```bash
rm -rf node_modules package-lock.json
npm install
```

**Q: Tauri 编译失败**

A: 检查 Rust 版本是否满足要求 (1.77.2+)：
```bash
rustc --version
rustup update
```

### 测试问题

**Q: 测试超时**

A: 增加测试超时时间或检查是否有异步操作未正确等待。

**Q: E2E 测试失败**

A: 确保应用已构建：
```bash
npm run build
```

### 性能问题

**Q: 大数据量时响应慢**

A:
- 使用分页和虚拟滚动
- 优化数据库查询
- 检查是否有不必要的重渲染

**Q: 内存占用高**

A:
- 检查是否有内存泄漏
- 清理不必要的缓存
- 优化大对象的存储

---

## 🤝 贡献指南

欢迎贡献代码！请遵循以下步骤：

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'feat: Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

### 代码审查标准

- 所有测试必须通过
- 代码覆盖率不低于 80%
- 遵循项目代码规范
- 提供清晰的提交信息

---

## 📄 许可证

本项目采用 MIT 许可证 - 详见 [LICENSE](LICENSE) 文件

---

## 📞 联系方式

- **项目主页**: [GitHub Repository](#)
- **问题反馈**: [Issues](#)
- **文档**: [docs/](docs/)

---

## 🙏 致谢

感谢以下开源项目：

- [Tauri](https://tauri.app/) - 跨平台桌面应用框架
- [React](https://react.dev/) - UI 框架
- [Ant Design](https://ant.design/) - UI 组件库
- [Vite](https://vitejs.dev/) - 构建工具
- [SeaORM](https://www.sea-ql.org/SeaORM/) - Rust ORM

---

**Built with ❤️ using Tauri + React + Rust**
